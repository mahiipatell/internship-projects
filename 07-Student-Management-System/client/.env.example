# Absolute URL of the deployed API. Only needed when the client is hosted
# separately from the server (e.g. Vercel + Render). Leave unset for same-origin
# deploys and local dev (the app defaults to the relative "/api" path).
VITE_API_URL=https://your-server.onrender.com
